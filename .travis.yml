language: java
jdk: openjdk8
sudo: false
script:
- jdk_switcher use openjdk8
- bash gradlew displayCommitHash
- bash gradlew isCI
- bash gradlew buildPlugin
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
