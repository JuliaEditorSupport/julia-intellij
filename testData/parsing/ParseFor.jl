s()=print(1)
a(s() for i in [1, 2, 3])
[]
for α ∈ [1, 2, 3, 4]
    println("α = $α while $(for β in [6, 6, 6]
        println(β)
    end)")
end

for j = 1:(i < lc ? 64 : last_chunk_len)
            if Imsk & u != 0
                lx < c && throw_setindex_mismatch(X, c)
                @inbounds x = convert(Bool, X[c])
                C = ifelse(x, C | u, C & ~u)
                c += 1
            end
            u <<= 1
        end